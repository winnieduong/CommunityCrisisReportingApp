name: community_crisis_reporting_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.5.1
  google_maps_flutter: ^2.0.6
  http: ^1.2.2
  geolocator: ^13.0.1
  flutter_local_notifications: ^18.0.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
